import { style } from '@vanilla-extract/css';

export const triggerButton = style({
  width: 'fit-content',
  height: 'fit-content',
});

export const trigger = style({
  width: '40px',
  height: '40px',
  cursor: 'pointer',
  margin: '0',
});
